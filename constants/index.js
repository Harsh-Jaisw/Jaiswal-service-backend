const common = require('./common')
const account = require('./account')
const category = require('./category')
const middleware = require('./middleware')
const faq = require('./faq')
module.exports = {account, common, category, middleware, faq}
